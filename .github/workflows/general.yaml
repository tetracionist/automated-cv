name: general
on:
  workflow_dispatch:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

jobs:
  prettier:
    name: prettier
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run prettier checks
        run: npm run format:check

  # super-linter:
  #   name: super-linter
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v6
  #     - name: Run super linter
  #       uses: super-linter/super-linter@v8.5.0
  #       env:
  #         DEFAULT_BRANCH: main
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         VALIDATE_ALL_CODEBASE: 'true'
  #         VALIDATE_BASH: 'true'
  #         VALIDATE_EDITORCONFIG: 'true'
  #         VALIDATE_MD: 'true'
  #         VALIDATE_YAML: 'true'
  #         FILTER_REGEX_EXCLUDE: '.*/(CHANGELOG\.md)'

  build-latex:
    name: build-latex
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Build resume.pdf
        run: make build
      - name: Upload resume.pdf
        uses: actions/upload-artifact@v7
        with:
          name: resume
          path: resume.pdf
          if-no-files-found: error

  release:
    name: release
    if: github.ref == 'refs/heads/main'
    needs:
      - prettier
      # - super-linter
      - build-latex
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Download resume.pdf from build-latex
        uses: actions/download-artifact@v8
        with:
          name: resume
          path: .
      - name: Normalize resume.pdf location
        run: |
          if [ -f resume/resume.pdf ]; then
            mv resume/resume.pdf ./resume.pdf
            rmdir resume
          fi
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
      - name: Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIT_AUTHOR_NAME: tetracionist
          GIT_AUTHOR_EMAIL: tetracionist@gmail.com
          GIT_COMMITTER_NAME: tetracionist
          GIT_COMMITTER_EMAIL: tetracionist@gmail.com
        run: |
          npx \
            -p @semantic-release/commit-analyzer \
            -p @semantic-release/release-notes-generator \
            -p conventional-changelog-conventionalcommits \
            -p @semantic-release/changelog \
            -p @semantic-release/git \
            -p @semantic-release/github \
            -p semantic-release \
            semantic-release

  publish-latest:
    name: publish-latest
    if: github.ref == 'refs/heads/main'
    needs:
      - build-latex
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download resume.pdf from build-latex
        uses: actions/download-artifact@v8
        with:
          name: resume
          path: .
      - name: Normalize resume.pdf location
        run: |
          if [ -f resume/resume.pdf ]; then
            mv resume/resume.pdf ./resume.pdf
            rmdir resume
          fi
      - name: Publish latest release asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest Resume
          files: resume.pdf
          draft: false
          prerelease: false

  deploy-gh-pages:
    name: deploy-gh-pages
    if: github.ref == 'refs/heads/main'
    needs:
      - release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download resume.pdf from build-latex
        uses: actions/download-artifact@v8
        with:
          name: resume
          path: .
      - name: Normalize resume.pdf location
        run: |
          if [ -f resume/resume.pdf ]; then
            mv resume/resume.pdf ./resume.pdf
            rmdir resume
          fi
      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: .
          force_orphan: true
          commit_message: 'release: update gh-pages to latest version'
